# Deployment

The current repository was exported from the deployed AWS environment. Before the next production deployment, consolidate the infrastructure into reviewed canonical templates and replace incremental browser-patch loaders with maintainable source modules.

Do not deploy directly from unreviewed local Downloads files.
